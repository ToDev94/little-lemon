import React from "react";
import Nav from "./Nav";

const Header = () => {
  return (
    <header>
      <img src="./Logo.svg" alt="logo" />
      <Nav />
    </header>
  );
};

export default Header;
